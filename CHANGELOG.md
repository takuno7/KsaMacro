# Changelog

모든 주요 변경 사항은 이 문서에 기록됩니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/)를 기반으로 하며,
버전 관리는 [Semantic Versioning](https://semver.org/spec/v2.0.0.html)을 따릅니다.

---

## [1.2.0] - 2026-09-20

### 추가
- 텔레그램 `🪪 카드 정보` 메뉴에서 카드번호·유효기간·비밀번호 앞 2자리·생년월일 변경 (입력 메시지는 봇이 즉시 삭제, 상태에는 끝 4자리만 표시)
- 프로그램 종료 시 텔레그램 메뉴 키보드 제거
- 자동 결제 꺼짐 시 **가상계좌 예약**까지 완료하고 입금은행·계좌번호·입금액·입금기한을 텔레그램과 GUI로 안내 (이력 상태 `가상계좌 입금대기`, 예매내역 링크 첨부)

### 변경
- 가상계좌를 지원하지 않는 운항편은 좌석을 잡지 않고 "🔔 빈자리 발견 (예약 안 됨)" 알림과 예매 페이지 링크만 보낸 뒤 매크로 중지 (좌석을 선점하면 약 20분간 본인도 직접 예매할 수 없어서). 예매 이력에는 남기지 않음
- 자동 결제를 끈 채 매크로를 시작하면 조회 범위 운항편의 가상계좌 지원 여부를 선사별로 미리 확인해, 미지원 운항편이 있으면 PC 팝업·로그·텔레그램으로 경고
- 좌석 롤백 요청 필드명을 사이트 규격(`Method`/`TicketingKey`/`GroupID`/`ApproveKind`)으로 통일
- 안내서(`시작하기_안내.html`)에 카드 정보 텔레그램 변경, 가상계좌 예약, 종료 시 메뉴 제거 안내 추가
- `🚢 여정 즉시 조회` 시 "잠시만 기다려주세요" 안내 메시지 제거

### 버그 수정
- **중복 결제·예약 방지**: 예매 성공 후 알림 작성이 실패하면 감시를 계속해 다시 예매하던 문제 수정
- **중복 결제·예약 방지**: 카드 승인 이후 발권 실패나 응답 불명(타임아웃), 가상계좌 발급·확정 응답 불명 시 자동 재시도하지 않고 매크로를 중지하며 예매내역 확인을 안내
- 텔레그램 결과 알림에 서버 값(선박명 등)을 HTML 이스케이프해 특수문자로 알림이 누락되지 않도록 수정
- 매크로 실행 중 텔레그램으로 카드 정보를 입력해도 입력 메시지를 삭제하도록 수정
- 텔레그램 카드번호 자릿수 검증을 GUI와 같은 14~16자리로 통일
- **보안**: 텔레그램 Bot Token이 `config.json`에 평문으로도 저장되던 문제 수정 (keyring 저장 성공 시 파일에서 제외)
- 매크로 시작·중지 시 텔레그램 알림이 없던 문제 수정 (시작 시 현재 설정 요약 전송), 시작이 입력 오류로 막히면 그 이유도 텔레그램으로 전송
- 매크로 실행 중 창을 닫으면 확인 후 진행 중인 예매 단계가 끝날 때까지 기다렸다 종료 (결제 도중 프로세스 종료 방지)

- **이중 결제 방지**: 카드 승인 응답의 `errCode`가 문자열 `"0"`이면 실패로 오판해 롤백 후 재결제하던 문제 수정. 결과 코드가 없으면 결과 불명으로 중지
- 카드 승인 거절 시 매 조회마다 재결제를 시도하던 문제 수정 (좌석 되돌린 뒤 매크로 중지·텔레그램 알림)
- 카드 발권 응답의 행별 `errcode` 실패를 성공으로 표시하던 문제 수정
- 좌석 배정 실패·응답 오류·일부만 배정 시 잡힌 좌석을 되돌리지 않던 문제 수정
- 잔여석이 승객 수보다 적은 객실을 골라 매번 실패하던 문제 수정 (유아 제외 필요 좌석 수 이상인 객실만 선택)
- 객실 데이터 하나가 이상하면 매 조회가 실패하던 문제 수정 (해당 객실만 건너뜀)
- 세션 만료 시 재로그인이 거의 일어나지 않던 문제 수정 (실패 시 실제 로그인 상태 확인 후 재로그인)
- 매크로 중복 시작 방지, 시작 시간 > 종료 시간이면 시작 차단
- 텔레그램 토큰 변경 후 재연동이 "연동 중..."에서 멈추던 문제, 연동 확인 중 재클릭 시 크래시 가능성 수정
- 연동 대기 중 봇에 여러 사람이 메시지를 보냈으면 자동 연동 거부 (제3자 페어링 방지)
- 오프라인 실행 시 저장된 출발/도착항 이름이 지워지던 문제 수정
- 승객 생년월일이 로그를 통해 텔레그램으로 전송되던 문제 수정, 작업 로그 HTML 이스케이프
- 설정·예매 이력 파일을 원자적으로 저장하고, 손상 시 `.bak`으로 보존해 덮어쓰지 않음
- 텔레그램 토큰은 keyring 저장 실패 시에도 `config.json`에 평문으로 남기지 않음
- 텔레그램 연결 오류(잘못된 토큰 등)를 작업 로그에 표시, 수정된 메시지로 인한 핸들러 오류 방지
- **화면 멈춤 해소**: 시작 시 항구 목록·자동 로그인, 출발항 변경, 로그인 확인, 여정 조회(버튼·텔레그램)를 백그라운드 단일 스레드에서 순서대로 실행 (창이 즉시 뜨고 조회 중에도 화면이 멈추지 않음). 텔레그램 출발항 변경·맞바꾸기는 도착항 목록을 받아 온 뒤 보고
- 예매 내역 창에서 표시하지도 않는 서버 조회를 제거 (이 PC 기록이 없으면 KSA 웹사이트 열기 안내)
- **보안 저장소 오류 알림**: Windows 자격 증명 관리자 읽기/쓰기 실패 시 조용히 넘기지 않고 로그와 팝업으로 알림, KSA 아이디를 바꾸거나 비밀번호 칸을 비우면 이전 비밀번호를 보안 저장소에서 삭제
- **2차 감사 반영**
  - 매크로 실행 중 승객을 추가·수정·삭제하면 실행 중인 매크로의 예매 명단이 바뀌던 문제 수정 (워커에 설정 사본 전달)
  - 카드 결제 후 발권이 명확히 실패하면 사이트와 같이 카드 결제를 자동 취소(`cardcancel`)한 뒤 좌석 반환, 취소 실패 시 고객센터 안내
  - 발권·예약 확정 응답에 결과 행이 없으면 성공으로 단정하지 않고 결과 불명으로 중지
  - 가상계좌 취소가 확인된 경우에만 좌석을 되돌리고, 취소 실패 시 "입금하지 말고 예매내역 확인" 안내
  - 가상계좌 정보 조회의 일시 오류를 "미지원 선사"로 오판해 빈자리 알림만 보내고 멈추던 문제 수정 (다음 조회에서 재시도)
  - 좌석 배정 결과 검증을 사이트와 동일하게 강화 (좌석번호 ≥ 0, 표시 좌석명 존재)
  - 소아·유아에게도 대인 할인코드를 넣던 문제 수정 (권종별 할인코드 사용)
  - 출발항 변경 직후 도착항 목록 로딩 중에 시작하면 새 출발항과 옛 도착항이 짝지어지던 문제 수정 (로딩 중 시작·저장 차단, 로드 실패 시 옛 목록 제거)
  - 텔레그램 재연동 후 원격 중지가 안 되던 문제, 이전 매크로의 늦은 종료 신호가 새 매크로 상태를 덮던 문제 수정
  - 텔레그램 설정 요약의 `<b>` 태그가 그대로 보이던 문제와 설정 메뉴 키보드가 주 메뉴로 바뀌던 문제 수정, 상태 조회가 한 박자 늦은 값을 보이던 문제 수정
  - 보안 저장소를 잠깐 못 읽었을 때 저장된 KSA 비밀번호가 삭제될 수 있던 문제 수정 (빈 칸으로 삭제하지 않음)
  - 설정 파일이 잠겨 있어 못 읽으면 기본값으로 덮어쓰지 않도록 그 실행에서는 저장 안 함, 백업 파일은 시각별로 보관
  - 봇 대화에 여러 사람이 있어 연동을 거부한 경우 이유를 화면에 표시, 종료 중 취소된 작업이 대화상자를 띄우지 않도록 수정
  - 군인·섬 주민 등 특수할인 정보(`eventcontents`)를 사이트와 같이 승객 검증 결과에서 받아 결제 요청에 반영
  - 좌석 되돌리기를 사이트와 동일하게 (카드 경로 좌석 배정 실패 시 2회, 가상계좌 발급 오류 시 발급된 그룹 번호로)
  - 텔레그램 재연동 시 이전 연결 종료를 기다리며 화면이 멈추던 문제 수정 (종료되면 새 연결 자동 시작)
  - 텔레그램 재연결 대기 중 보낸 알림이 쌓였다가 늦게 도착하던 문제 수정 (HTTP API로 즉시 전송), 실패한 연결 세션 자원 정리
  - 승객 정보에 **비상연락처** 입력칸 추가 (KSA가 운항편 설정 `requiredemtel`로 요구, 조회한 노선 모두 필수). 미입력·본인 번호와 동일·승객 간 번호 중복이면 매크로 시작 전에 안내
  - **보안**: 텔레그램 연결 오류·전송 오류 로그와 라이브러리(httpx) 요청 로그에 봇 토큰이 그대로 기록되던 문제 수정
- 릴리즈 스크립트가 전체 테스트를 실행하도록 변경, `.omc/`를 `.gitignore`에 추가

### 추가 (안전장치)
- 결과 불명으로 매크로가 멈추면 시작 시점 예매내역과 현재 예매내역을 자동 대조해 새 예약/결제 여부를 알림에 포함
- 예매 내역 창 상태 칸에 가상계좌 은행·계좌번호·입금기한 표시
- 알림의 `티켓키`를 `관리번호 (입력할 곳 없음)`로 바꿔 사이트에 입력해야 하는 값으로 오해하지 않도록 수정
- 텔레그램 카드 정보 메뉴에 전송 경로(텔레그램 서버·알림 미리보기) 경고 추가
- 안내서(`시작하기_안내.html`)를 화면 항목별 설명, 동작 순서, 결제 방식 비교, 가상계좌 입금 절차, 텔레그램 메뉴 전체 표, 알림 종류와 대처, 보안, 문제 해결 중심으로 전면 보강

---

## [1.1.0] - 2026-09-20

### 추가
- 텔레그램 `⚙️ 예매 설정` 메뉴에서 출발항·도착항·날짜·조회 시간·객실 유형·조회 간격·자동 결제를 변경하도록 지원
- 출발항 검색 후보와 연결 가능한 도착항을 버튼으로 제공하고 출발·도착항 맞바꾸기 지원
- 텔레그램 설정 변경을 GUI 위젯과 설정 파일에 즉시 반영하고 갱신 상태를 회신
- 매크로 실행 중 설정 변경 차단

---

## [1.0.2] - 2026-09-20

### 버그 수정
- 텔레그램의 `🚢 여정 즉시 조회` 버튼이 실제 조회를 실행하고 결과를 채팅으로 반환하도록 수정

---

## [1.0.1] - 2026-09-16

### 🔧 보안 강화 및 버그 수정 / UI 리파인
- **자격 증명 보안 강화**:
  - 기본 설정 및 소스코드 전반에서 예시 계정 하드코딩 완전 제거 (빈 문자열 기본값 적용)
  - 사용자가 GUI에서 직접 입력한 정보만 암호화 저장소(`keyring`)에 안전 보관되도록 조치
- **TrainMacro 방식 즉시 시작 시퀀스 개선**:
  - 앱 기동 시 텔레그램 원격 제어 리스너 및 백그라운드 자동 로그인을 즉시 가동하여 시작하자마자 스마트폰 원격 제어 가능
- **UI 시각 디자인 개선**:
  - `QGroupBox::title` 배경색을 윈도우 배경과 일치시켜 부자연스러운 사각 흰색 패치 이질감 완벽 제거
  - `[➕ 여객 추가]`, `[👶 유아 추가]`, `[선택 삭제]` 버튼 간격을 12px로 넓혀 조작성과 시인성 향상
- **실예매 트랜잭션 오류 픽스**:
  - 여정 선택 확인(`selectCheckJourney`) API의 페이로드 규격을 KSA 공식 `ksa.util.journeyChkMaker` 구조(`rf_portid`, `rt_portid`, `pairtime`, `step`, `classesList` 등)로 100% 일치시켜 `Expecting value: line 3 column 1 (char 4)` SQL 에러 완벽 해결

---

## [1.0.0] - 2026-09-16

### 🎉 최초 릴리즈 (Initial Release)

- **KSA(한국해운조합 - 가보고 싶은 섬) 여객선 전용 리버스 엔지니어링 API 엔진 구축 (`engines.py`)**
  - 공개 API가 없는 KSA 여객선 예매 사이트(`island.theksa.co.kr`)의 백엔드 통신 프로토콜을 완벽 분석 및 구현
  - 세션 기반 초고속 HTTP 직접 통신으로 브라우저 없이 수십 밀리초(ms) 단위의 초고속 예매 처리
  - 전국 300+ 출발항 및 출발항별 연결 도착항 실시간 동적 조회
  - 실시간 운항 일정표, 객실 등급별(일반객실, 우등실 등) 잔여석 및 운임 정보 조회
  - 여정 검증, 승객 유효성 검증 및 임시 등록, 좌석 배정/락, 카드 결제 승인, 최종 발권 완벽 자동화

- **로그인 기억 및 자동 복원 기능 (`config_manager.py`, `ksa_macro_main.py`)**
  - KSA 회원 계정(ID)과 일반 설정의 `%APPDATA%/KsaMacro/config.json` 영구 보관
  - 비밀번호, 카드 정보, 텔레그램 토큰을 Windows Credential Manager (`keyring`)에 안전 암호화 저장
  - 앱 시작 시 계정 정보를 자동 로드하고 백그라운드에서 **자동 로그인 세션 활성화**
  - UI 입력 필드 및 설정 변경 시 실시간 자동 저장

- **비동기 매크로 워커 (`macro_worker.py`)**
  - `QThread` 기반 백그라운드 폴링으로 UI 멈춤 없이 1~2초 주기로 잔여석 지속 감시
  - 객실 선호도 필터링: '일반객실 우선', '일반객실만', '전체 객실' 지원
  - 취소표/잔여석 발견 즉시 자동 예약 및 카드 결제 트랜잭션 수행

- **PyQt6 네이티브 라이트 테마 UI 데스크톱 애플리케이션 (`ksa_macro_main.py`)**
  - TrainMacro와 완벽히 동일한 깔끔하고 시인성 높은 네이티브 라이트 테마 스타일 적용
  - 출발지 선택 시 연결 도착지 자동 필터링 및 출·도착지 맞바꿈(`⇄`) 기능
  - 실시간 운항 시간표 및 잔여석 현황을 즉시 확인할 수 있는 [실시간 여정 조회] 기능
  - 승객 등록 체계 개선: 주민등록번호 뒷자리 수집 폐지에 맞춘 **생년월일 8자리(YYYYMMDD)** 입력 지원
  - **여객 추가(대인/소아/경로)** 와 **유아 추가(만 2세 미만 무임, 0원)** 분리 등록 지원 및 전용 다이얼로그 제공
  - 상세한 이벤트 추적을 위한 실시간 컬러 로그 뷰어
  - 확정된 승선권을 확인할 수 있는 [나의 예매내역 조회] 기능

- **TrainMacro 방식 텔레그램 연동 및 스마트폰 원격 제어 키보드 메뉴 지원 (`telegram_remote.py`)**
  - TrainMacro와 100% 동일한 **Bot Token 단일 입력 + [연동하기] 버튼** 간소화 UI
  - 최근 대화 기록 조회를 통한 **Chat ID 자동 감지(오토 페어링)** 지원 (수동 입력 불필요)
  - 상세한 텔레그램 봇 생성 및 연동 가이드 **풍선 도움말(툴팁)** 제공
  - 모바일 텔레그램 채팅창에 원격 제어 키보드 버튼 메뉴(`▶️ 매크로 시작`, `⏹️ 매크로 중지`, `🔍 현재 설정 확인`, `🚢 여정 즉시 조회`) 제공
  - 예매 성공 시 선박명, 출발시간, 좌석번호, 결제금액, 승객 목록 텔레그램 즉시 전송

- **독립 실행 파일 (`KsaMacro.exe`) 배포 지원**
  - PyInstaller를 통해 파이썬이 설치되지 않은 환경에서도 더블 클릭으로 실행 가능한 단일 포터블 exe 파일 제공
